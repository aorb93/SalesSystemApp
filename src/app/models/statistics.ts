export interface BestProduct{
    productName: string,
    quantity: number
}

export interface BestClient{
    clientName: string,
    quantity: number
}

export interface SalesMonths{
    year: number,
    shortName: string,
    longName: string,
    quantity: number,
    total: number
}